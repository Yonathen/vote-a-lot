export enum Actions {
  ResetPoll = 'Reset Poll',
  CreatePoll = 'Create poll',
  UpdatePoll = 'Update poll',
  AddOption = 'Add option',
  RemoveOption = 'Remove option',
  UpdateOption = 'Update option',
  VoteForOption = 'Vote for option'
}
